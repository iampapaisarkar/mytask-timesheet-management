/** Re-export shared map chrome styles for backwards-compatible imports. */
export {
  MINIMAL_MAP_STYLE,
  MINIMAL_MAP_STYLE_DARK,
  MINIMAL_MAP_STYLE_LIGHT,
  mapStyleForTheme,
  type MapThemeMode,
} from "@mytask/theme";
