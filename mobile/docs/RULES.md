# Mobile Rules

- Feature parity with web where practical.
- Prefer maintained React Native community packages over one-off native modules.
- This app uses **React Native CLI** (bare `ios/` + `android/`).
- Never introduce Expo dependencies or Expo config files.
