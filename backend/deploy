#!/usr/bin/env bash
# Legacy mysheet-prod / Australia Cloud Build entrypoint — removed.
# Use the myTask Cloud Run container deploy script instead:
#
#   cp .env.cloudrun.example .env.cloudrun   # once
#   ./deploy-cloud-run.sh
#
# See docs/DEPLOY_CLOUD_RUN.md
echo "This legacy ./deploy script targeted mysheet-prod (Australia)."
echo "Use: ./deploy-cloud-run.sh"
exit 1
